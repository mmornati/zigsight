# Local development environment: unit tests against the LATEST Home Assistant
# release plus the lint / type-check tools. Requires Python 3.14.
#
# To test against the minimum supported Home Assistant release instead
# (Python 3.13), install requirements-test-min.txt + requirements-lint.txt in
# a separate virtualenv -- see CONTRIBUTING.md ("Testing against the minimum
# and latest Home Assistant").
-r requirements-test-latest.txt
-r requirements-lint.txt
